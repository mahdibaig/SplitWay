import SwiftUI

extension Font {
    static let bigNumber  = Font.system(size: 40, weight: .medium, design: .default)
    static let cardTitle  = Font.system(size: 17, weight: .semibold)
    static let cardLabel  = Font.system(size: 13, weight: .regular)
    static let serifHero  = Font.system(.largeTitle, design: .serif).italic()
    static let serifTitle = Font.system(.title2,    design: .serif).italic()
}
